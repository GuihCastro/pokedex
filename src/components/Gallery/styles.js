import styled from "styled-components";

export const Container = styled.div`
    grid-area: gallery;

    width: 100%;
    min-height: 100vh;
    padding: 25rem 3rem 0;

    display: grid;

    overflow-y: auto;
`;